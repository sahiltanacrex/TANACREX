<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="email_template_edi_sale_inherit" model="mail.template">
            <field name="name">Sales Order: Send by email inherit</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">{{ object.company_id.name }} {{ object.state in ('draft', 'sent') and (ctx.get('proforma') and 'Proforma' or 'Quotation') or 'Order' }} (Ref {{ object.name or 'n/a' }})</field>
            <field name="email_from">{{ (object.user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        ATTN :
                        <t t-if="object.partner_id.parent_id">
                            <t t-out="object.partner_id.name or ''">Brandon Freeman</t> (<t t-out="object.partner_id.parent_id.name or ''">Azure Interior</t>),
                        </t>
                        <t t-else="">
                            <t t-out="object.partner_id.name or ''">Brandon Freeman</t>,
                        </t>
                        <br/><br/>
                        <t t-set="doc_name" t-value="'quotation' if object.state in ('draft', 'sent') else 'order'"/>
                        Dear Sir/Madam,
                        <br/><br/>
                        Please find enclosed the
                        <t t-if="ctx.get('proforma')">
                            PROFORMAT invoice <t t-out="doc_name or ''"></t> <strong t-out="object.name or ''">S00052</strong>
                            <t t-if="object.sale_order_partner">
                                (reference: <t t-out="object.sale_order_partner or ''"></t> )
                            </t>
                            amounting to a total of <strong t-out="format_amount(object.amount_total, object.pricelist_id.currency_id) or ''">$ 10.00</strong>.
                        </t>
                        <t t-else="">
                            <t t-out="doc_name or ''">quotation</t> <strong t-out="object.name or ''"></strong>
                            <t t-if="object.sale_order_partner">
                                (reference: <t t-out="object.sale_order_partner or ''">S00052</t> )
                            </t>
                            amounting to a total of <strong t-out="format_amount(object.amount_total, object.pricelist_id.currency_id) or ''">$ 10.00</strong>.
                        </t>
                        <br/><br/>
                        We look forward to receiving your approval and confirmation,
                        <br/><br/>
                        Best Regards,
                        <t t-if="object.user_id.signature">
                            <br />
                            <t t-out="object.user_id.signature or ''">--<br/>Mitchell Admin</t>
                        </t>
                    </p>
                </div>
            </field>
            <field name="report_template" ref="sale.action_report_saleorder"/>
            <field name="report_name">{{ (object.name or '').replace('/','_') }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

    </data>
</odoo>
