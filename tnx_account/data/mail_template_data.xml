<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <data>
        <!--Email template -->
        <record id="email_template_edi_invoice_inherit" model="mail.template">
            <field name="name">Invoice: Send by email inherit</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="email_from">{{ (object.invoice_user_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="subject">{{ object.company_id.name }} Invoice (Ref {{ object.name or 'n/a' }})</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        ATTN :
                        <t t-if="object.partner_id.parent_id">
                            <t t-out="object.partner_id.name or ''">Brandon Freeman</t> (<t t-out="object.partner_id.parent_id.name or ''">Azure Interior</t>),
                        </t>
                        <t t-else="">
                            <t t-out="object.partner_id.name or ''">Brandon Freeman</t>,
                        </t>
                        <br /><br />
                        Dear Sir/Madam,
                        <br /><br />
                        Please find attached the
                        <t t-if="object.name">
                            invoice <strong t-out="object.name or ''">INV/2021/05/0005</strong>
                        </t>
                        <t t-else="">
                            invoice
                        </t>
                        <t t-if="object.origin_sale_id.sale_order_partner">
                            (with reference: <t t-out="object.origin_sale_id.sale_order_partner or ''">SUB003</t>)
                        </t>
                        amounting to a total of <strong t-out="format_amount(object.amount_total, object.currency_id) or ''">$ 143,750.00</strong>

                        . The invoice is due by : <strong t-out="object.invoice_date_due.strftime('%d/%m/%Y') or ''"></strong>
                        <br /><br />
                        We look forward to hearing from you soon,
                        <br /><br />
                        Best Regards,
                        <t t-if="object.invoice_user_id.signature">
                            <br />
                            <t t-out="object.invoice_user_id.signature or ''">--<br/>Mitchell Admin</t>
                        </t>
                    </p>
                </div>
            </field>
            <field name="report_template" ref="account.account_invoices"/>
            <field name="report_name">Invoice_{{ (object.name or '').replace('/','_') }}{{ object.state == 'draft' and '_draft' or '' }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="auto_delete" eval="True"/>
        </record>

    </data>
</odoo>
